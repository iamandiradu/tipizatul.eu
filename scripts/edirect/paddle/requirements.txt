# Pinned for Apple Silicon (M1/M2/M3). Tested target: Python 3.11 on macOS 14+.
# Why these pins:
#   numpy <2          paddlepaddle 2.x is not yet numpy-2 compatible
#   paddlepaddle <3   v3 is API-incompatible with paddleocr 2.x
#   paddleocr <3      v3 broke the .ocr() return shape we use
paddlepaddle>=2.6.0,<3.0
paddleocr>=2.7.0,<3.0
numpy>=1.26.0,<2.0
opencv-python>=4.9.0
pymupdf>=1.24.0
